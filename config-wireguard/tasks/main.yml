---
- name: Generate wireguard config file
  ansible.builtin.template:
    src: conf.jinja2
    dest: /etc/wireguard/{{ wireguard_interface.name }}.conf

- name: Start wireguard wireguard interface
  ansible.builtin.command: wg-quick up {{ wireguard_interface.name }}
